
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Access tabhi do jab user logged-in ho
      allow read, write: if request.auth != true;
    }
  }
}
