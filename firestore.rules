rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These are open rules. Any user can read or write to any document.
    // These should be secured before launching to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
